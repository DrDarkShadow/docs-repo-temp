## Overview

This script is a basic Python program whose sole purpose is to print the string "hello" to the standard output. It serves as a minimal example of an executable Python file and does not contain any classes, functions, or complex logic.

## Usage

You can execute this script from a command-line interface using the Python interpreter.

**Example:**

```bash
python change.py
```

### Expected Output

When the script is run, it will print the following message to the console:

```
hello
```